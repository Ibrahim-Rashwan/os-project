apiVersion: apps/v1
kind: Deployment
metadata:
  name: os-project-deployment
spec:
  replicas: 3

  selector:
    matchLabels:
      tag: os-project

  template:
    metadata:
      name: os-project-pod
      labels:
        tag: os-project
    spec:
      containers:
        - name: main
          image: ibrahim9150/os-project-image
